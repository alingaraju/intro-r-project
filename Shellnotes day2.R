#Shell Notes

#Exercise
#1. Create a directory
#mkdir some_directory
#2. Create afile in that directory
#touch some_directroy/testfile.txt
#OR
#cd some_directory
#touch testfile.txt

#Open file in nano and make changes
#nano testfile.txt

#(Ctrl 0) to save
#(Ctrl X) to quit


#4 navigate back out of that directory
#cd ..
#5. delete the directory
#rm -r some_directory






#Exercise:
# Provide a shell command that find the 3 files have the "least" number of lines in a directory
#HINT : think about what the "*" wildcard means in english
# Write the same shell command to find the 3 with the most lines




#Exercise
# 1. FInd the number of lines for each .csv, .txt and .md file in "data/"















































